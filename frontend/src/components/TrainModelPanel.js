// TrainModelPanel removed: training UI disabled per product decision.
// Kept placeholder file to avoid stray import breakages in older branches.
export default function TrainModelPanelPlaceholder() {
  return null;
}

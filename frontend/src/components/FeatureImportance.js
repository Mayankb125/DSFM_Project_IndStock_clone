import React from 'react';

// FeatureImportance visualization removed along with model training code.
// Keep a placeholder component to avoid missing-import errors.
const FeatureImportance = () => null;

export default FeatureImportance;

// GarchPanel removed - this file replaced with a placeholder to avoid module-not-found errors.

// If you need to restore the GARCH diagnostic UI, recreate this component or remove the import from Dashboard.

export default null;
